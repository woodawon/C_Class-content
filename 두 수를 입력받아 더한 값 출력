#include <stdio.h> 

main() {
	
	int a, b, num, top, bottom, sum = 0;
	scanf("%d %d", &a, &b); 
	
	if(a > b)
		top = a, bottom = b;
	else
		top = b, bottom = a;
	num = bottom;  
	
	do {
		sum += num;  
		num += 1;
	} while(num <= top);
	printf("%d", sum);
	
}
